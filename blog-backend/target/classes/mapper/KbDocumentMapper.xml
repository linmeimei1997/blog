<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.KbDocumentMapper">

    <resultMap id="BaseResultMap" type="com.blog.entity.KbDocument">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="file_name" property="fileName"/>
        <result column="file_type" property="fileType"/>
        <result column="file_size" property="fileSize"/>
        <result column="file_path" property="filePath"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="chunk_count" property="chunkCount"/>
        <result column="upload_by" property="uploadBy"/>
        <result column="upload_time" property="uploadTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, file_name, file_type, file_size, file_path, content, status,
        chunk_count, upload_by, upload_time, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM kb_document
        WHERE id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM kb_document
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%') OR file_name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO kb_document (title, file_name, file_type, file_size, file_path, content,
                                status, chunk_count, upload_by, upload_time, create_time, update_time)
        VALUES (#{title}, #{fileName}, #{fileType}, #{fileSize}, #{filePath}, #{content},
                #{status}, 0, #{uploadBy}, NOW(), NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE kb_document
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM kb_document WHERE id = #{id}
    </delete>

    <update id="updateChunkCount">
        UPDATE kb_document SET chunk_count = #{chunkCount}, update_time = NOW() WHERE id = #{id}
    </update>

</mapper>
