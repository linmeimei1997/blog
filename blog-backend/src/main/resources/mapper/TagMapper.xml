<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.TagMapper">

    <resultMap id="BaseResultMap" type="com.blog.entity.Tag">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="article_count" property="articleCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, article_count, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tag
        WHERE id = #{id}
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tag
        WHERE name = #{name}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM tag
        ORDER BY article_count DESC, create_time DESC
    </select>

    <select id="selectByArticleId" resultMap="BaseResultMap">
        SELECT t.<include refid="Base_Column_List"/>
        FROM tag t
        INNER JOIN article_tag at ON t.id = at.tag_id
        WHERE at.article_id = #{articleId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tag (name, article_count, create_time, update_time)
        VALUES (#{name}, 0, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE tag
        <set>
            <if test="name != null">name = #{name},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM tag WHERE id = #{id}
    </delete>

    <update id="incrementArticleCount">
        UPDATE tag SET article_count = article_count + 1 WHERE id = #{id}
    </update>

</mapper>
