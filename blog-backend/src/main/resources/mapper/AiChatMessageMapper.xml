<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.AiChatMessageMapper">

    <resultMap id="BaseResultMap" type="com.blog.entity.AiChatMessage">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="user_id" property="userId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="message_type" property="messageType"/>
        <result column="tools_used" property="toolsUsed"/>
        <result column="tokens" property="tokens"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, session_id, user_id, role, content, message_type, tools_used, tokens, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_chat_message
        WHERE id = #{id}
    </select>

    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_chat_message
        WHERE session_id = #{sessionId}
        ORDER BY create_time ASC
    </select>

    <select id="selectRecentBySessionId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_chat_message
        WHERE session_id = #{sessionId}
        ORDER BY create_time DESC
        LIMIT #{limit}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_message (session_id, user_id, role, content, message_type, tools_used, tokens, create_time)
        VALUES (#{sessionId}, #{userId}, #{role}, #{content}, #{messageType}, #{toolsUsed}, #{tokens}, NOW())
    </insert>

    <delete id="deleteBySessionId">
        DELETE FROM ai_chat_message WHERE session_id = #{sessionId}
    </delete>

</mapper>
